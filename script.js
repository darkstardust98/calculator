function add(x, n) {
    accumulator = x;
    return accumulator += n;
}

function subtract(x, n) {
    accumulator = x;
    return accumulator -= n;
}

function multiply(x, n) {
    accumulator = x;
    return accumulator * n;
}

function divide(x, n) {
    accumulator = x;
    return accumulator / x;
}

let first_number = 0;
let operator = 0;
let second_number = 0;

